services:
  netflix-analysis:
    image: yuhan023/netflix-analysis:latest
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./data:/app/data:ro
      - ./output:/app/output
    command: python main.py --data data/netflix_titles.csv --output output/figures
